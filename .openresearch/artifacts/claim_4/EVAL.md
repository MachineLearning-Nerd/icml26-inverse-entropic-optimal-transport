# Claim 4 evaluation

Formal result pending. The verifier is
`reproduction.swiss_calibration._practical_benchmark`; the canonical suite
exits nonzero if its parity, sampling, finiteness, or negative-control
contract fails. No verdict is assigned before the formal run.
